rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Sign language videos - public read access
    match /sign-videos/{allPaths=**} {
      allow read: if true;
      allow write: if false;  // Admin upload only
    }
  }
}
