// Example environment configuration
// Copy this to env.js and replace with your actual API key for local development
// On Vercel, the build.sh script generates this file automatically from environment variables

window.ENV = {
  GEMINI_API_KEY: "YOUR_GEMINI_API_KEY_HERE"
};
